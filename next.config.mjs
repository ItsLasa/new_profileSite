/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        remotePatterns: [{
         
          hostname: 'sdk.bitmoji.com',
         
        }]
      },
};

export default nextConfig;
